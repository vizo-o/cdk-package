{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "noEmit": true,
        "types": ["jest", "node"]
    },
    "include": ["lib/**/*.ts", "test/**/*.ts"],
    "exclude": ["node_modules", "dist", "cdk.out"]
}
